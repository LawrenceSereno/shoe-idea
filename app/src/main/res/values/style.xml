<resources>

    <style name="SizeButton">
        <item name="android:background">@drawable/size_button_background</item>
        <item name="android:backgroundTint">@null</item> <!-- ⬅️ this kills the tint -->
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textSize">14sp</item>
        <item name="android:layout_width">48dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_marginEnd">8dp</item>
        <item name="android:minHeight">0dp</item>
        <item name="android:minWidth">0dp</item>
        <item name="android:padding">0dp</item>
        <item name="android:foreground">@null</item> <!-- disables ripple -->
    </style>

    <!-- App theme using MaterialComponents -->
    <style name="Theme.RecycleViewTesting" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary & secondary branding colors -->
        <item name="colorPrimary">#000000</item>         <!-- Black -->
        <item name="colorPrimaryVariant">#000000</item>
        <item name="colorOnPrimary">#FFFFFF</item>       <!-- White text on black -->

        <item name="colorSecondary">#000000</item>
        <item name="colorSecondaryVariant">#000000</item>
        <item name="colorOnSecondary">#FFFFFF</item>

        <!-- Background -->
        <item name="android:windowBackground">#FFFFFF</item> <!-- White background -->
        <item name="android:colorBackground">#FFFFFF</item>

        <!-- Make status bar transparent -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item> <!-- Optional: Makes the status bar truly transparent -->

        <!-- Light status bar icons (for visibility on transparent background) -->
        <item name="android:windowLightStatusBar">true</item> <!-- Use light-colored icons (e.g., black) -->
    </style>

    <style name="SizeButtons" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:layout_width">56dp</item>
        <item name="android:layout_height">56dp</item>
        <item name="android:layout_marginEnd">8dp</item>
        <item name="android:background">@drawable/size_button_background</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>
</resources>
